defaults:
  - grappa-1.4.0 # includes the grappa-1.4.0 datasets, set datasets = [] to train only on the extra datasets
  - default

extra_datasets: []
extra_train_datasets: []
extra_val_datasets: []
extra_test_datasets: []