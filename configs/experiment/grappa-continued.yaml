defaults:
  - default

ckpt_path: models/grappa-1.4.0/checkpoint.ckpt

wandb:
  name: baseline
  project: grappa-continued
trainer:
  max_epochs: null
train:
  lr: 1e-05
  start_qm_epochs: 0
  param_loss_epochs: 0
  warmup_steps: 1000
  reset_optimizer_on_load: True
  add_noise_on_load: 0.0